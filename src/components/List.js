import React from 'react';
import axios from 'axios';

const ROOT_ENDPOINT = 'http://localhost:3001';

const List = ({}) => {

    return (
        <div>
            
        </div>
    )
}

export default List;